# JavaScript 

# JavaScript Fundamentals Roadmap

If you're starting with JavaScript, the best approach is to learn it in a logical progression. Here's a roadmap of **JavaScript Fundamentals** that builds from beginner to intermediate.

---

## 1. Introduction to JavaScript

- What is JavaScript?
- How JavaScript works in the browser
- Adding JavaScript to HTML
  - Inline
  - Internal
  - External
- Developer Console
- Comments

---

## 2. Variables and Data Types

- Variables (`let`, `const`, `var`)
- Naming rules
- Primitive data types
  - String
  - Number
  - Boolean
  - Undefined
  - Null
  - BigInt
  - Symbol
- Non-primitive data type
  - Object
- Type conversion
- Type coercion
- `typeof` operator

---

## 3. Operators

- Arithmetic Operators
- Assignment Operators
- Comparison Operators
- Logical Operators
- Bitwise Operators *(basic understanding)*
- Ternary Operator
- Nullish Coalescing (`??`)
- Optional Chaining (`?.`)

---

## 4. Input and Output

- `console.log()`
- `console.error()`
- `console.table()`
- `alert()`
- `prompt()`
- `confirm()`

---

## 5. Control Flow

### Conditional Statements

- `if`
- `if...else`
- `else if`
- `switch`

### Loops

- `for`
- `while`
- `do...while`
- `break`
- `continue`

---

## 6. Functions

- Function declaration
- Function expression
- Arrow functions
- Parameters
- Arguments
- Return values
- Default parameters
- Rest parameters (`...`)
- Scope
- Hoisting
- Callback functions

---

## 7. Strings

- Creating strings
- String methods
- Template literals
- Escape characters
- String interpolation

### Common Methods

- `length`
- `slice()`
- `substring()`
- `replace()`
- `includes()`
- `startsWith()`
- `endsWith()`
- `split()`
- `trim()`
- `toUpperCase()`
- `toLowerCase()`

---

## 8. Arrays

- Creating arrays
- Accessing elements
- Updating elements
- Array length

### Methods

- `push()`
- `pop()`
- `shift()`
- `unshift()`
- `slice()`
- `splice()`
- `concat()`
- `join()`
- `indexOf()`
- `includes()`

### Iteration

- `for`
- `for...of`
- `forEach()`

### Higher-Order Methods

- `map()`
- `filter()`
- `reduce()`
- `find()`
- `findIndex()`
- `some()`
- `every()`
- `sort()`

---

## 9. Objects

- Creating objects
- Properties
- Methods
- Nested objects
- Dot notation
- Bracket notation
- Object destructuring
- Spread operator

### Object Methods

- `Object.keys()`
- `Object.values()`
- `Object.entries()`
- `Object.assign()`

---

## 10. Scope

- Global scope
- Local scope
- Block scope
- Function scope
- Lexical scope

---

## 11. Execution Context

- Call stack
- Memory creation phase
- Execution phase
- Hoisting

---

## 12. Closures

- What is a closure?
- Practical examples
- Private variables

---

## 13. `this` Keyword

- Global context
- Object methods
- Arrow functions
- Regular functions
- `call()`
- `apply()`
- `bind()`

---

## 14. Error Handling

- `try`
- `catch`
- `finally`
- `throw`
- Creating custom errors

---

## 15. Dates and Math

### Math

- `Math.random()`
- `Math.floor()`
- `Math.ceil()`
- `Math.round()`
- `Math.max()`
- `Math.min()`

### Date

- Creating dates
- Current date/time
- Formatting dates

---

## 16. ES6+ Features

- `let`
- `const`
- Arrow functions
- Template literals
- Destructuring
- Spread operator
- Rest operator
- Default parameters
- Modules
- Classes *(basic)*
- Optional chaining
- Nullish coalescing

---

## 17. DOM Basics (Beginning of Web Development)

- Selecting elements
- Changing text
- Changing HTML
- Changing CSS
- Creating elements
- Removing elements
- DOM traversal

---

## 18. Events

- Mouse events
- Keyboard events
- Form events
- Event listeners
- Event bubbling
- Event delegation
- Prevent default

---

## 19. Asynchronous JavaScript (Essential)

- Synchronous vs Asynchronous

### Timers

- `setTimeout()`
- `setInterval()`

### Other Topics

- Callbacks
- Promises
- Promise chaining
- `async/await`
- Fetch API
- JSON

---

## 20. Modules

- Export
- Import
- Named exports
- Default exports

---

# Mini Projects After Fundamentals

Practice with small projects after finishing the fundamentals:

- Calculator
- To-Do List
- Counter App
- Stopwatch
- Digital Clock
- Random Quote Generator
- Weather App *(using APIs)*
- Quiz App
- Expense Tracker
- Password Generator

---

# Recommended Learning Order

1. Variables & Data Types
2. Operators
3. Conditionals
4. Loops
5. Functions
6. Arrays
7. Objects
8. Scope & Closures
9. `this`
10. ES6+ Features
11. Error Handling
12. DOM
13. Events
14. Asynchronous JavaScript
15. Modules
16. Build Projects